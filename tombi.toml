#:schema https://www.schemastore.org/tombi.json

[files]
exclude = [
  "**/uv.lock",
  "**/_deps/**",
  "**/_build/**",
  "**/dist/**",
  "**/.venv/**"
]

[schema]
# scikit-build-core の [[tool.dynamic-metadata]] など、tombi / SchemaStore が
# まだ正しく扱えない構成があるためスキーマ検証は無効化する
enabled = false
