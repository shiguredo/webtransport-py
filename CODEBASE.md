# CODEBASE

- デザインは DESIGN.md を確認すること
- 良い設計は良いコードを生む
- 徹底的に評価をしていく
- 徹底的に時間をかけて継続的に改善をしていく
- 破壊的変更は積極的に行う
- 設計変更も積極的に行う
- 良い設計、良い実装のためには積極的に破壊的変更をすること
- 下位互換を維持しないこと
- 調査メモなどは docs/ 以下に保存していく
- 間違いを認めよりよい修正をする
- この指示がなくなるまでは変更履歴を `CHANGES.md` に残さないこと
- この指示がなくなるまではPull-Request やブランチを作らず develop にコミットしていくこと
  - 1 Issue 1 コミット 1 プッシュ
- このプロダクトは E2E テスト向けのライブラリである。E2E テスト目的に利用できる
  よう API を充実させること。実装の内部事情に踏み込んだ細かい機能 (プロトコル
  レベルの制御と観測、異常系や競合の再現、ワイヤ単位の検証) を実現できる必要が
  あるため、公開 API とテスト専用 API のどちらであっても、細粒度の機能を
  惜しまず用意すること
- nghttp2 / nghttp3 / ngtcp2 をフォークしないこと。依存ライブラリの改修が必要な機能は、ライブラリ側の対応を待つか、バインディング層で吸収できる設計にすること

## C コールバック境界の例外方針

ngtcp2 / nghttp3 / nghttp2 / BoringSSL へ登録する C コールバック (`*_cb` /
`*_callback`) は、C++ の例外を境界の外へ出してはならない。例外が C のフレームを
巻き戻してライブラリ内部を壊し、プロセスが abort するためである。

- すべての C コールバックに `noexcept` を付与する (宣言と定義の両方)。Python を
  呼ばないコールバックは捕捉の追加を伴わない注釈のみの変更になる。C コールバック
  は `*_cb` / `*_callback` と命名する (機械検査の前提)。接尾辞を持たない関数
  ポインタ枠 (`ngtcp2_settings::log_printf` など) は命名規則の外にあり機械検査の
  対象外であるため、設定する場合は手動で確認する
- Python コールバックを呼ぶコールバックは、例外を送出し得る処理を含むため、
  外周で `try` / `catch` して握り、ライブラリのエラー値
  (`NGTCP2_ERR_CALLBACK_FAILURE` / `NGHTTP3_ERR_CALLBACK_FAILURE` /
  `NGHTTP2_ERR_CALLBACK_FAILURE`) を返す。`noexcept` を付けたまま例外を外へ出すと
  `std::terminate` になるため、捕捉は必須である
- 現状で Python コールバックを受け取るのは `quic.Config.verify_callback` のみで、
  対応する `QuicConnection::custom_verify_cb` (`src/bindings/quic.cpp`) がこの方針を
  実装している。Python コールバックを追加する場合は同じ形で捕捉すること
- コールバックから Python へ戻る経路 (nanobind) は `nb::lock_self()` 等で
  オブジェクトの排他を取る。コールバック内から同一オブジェクトのメソッドを
  呼ぶとデッドロックし得るため、再入可能な設計にするか呼び出しを禁じる
- 対象は名前付き関数に限らない。BoringSSL へ登録するラムダと `ngtcp2_crypto` が
  参照する関数ポインタも C のフレームへ例外を出し得るため、C のコールバックとして
  登録する無名ラムダにも `noexcept` を付与する (nanobind の `.def` に渡すラムダは
  Python へ例外を伝播させる必要があるため付けない)
- 例外の捕捉は、Python コールバックを呼ぶコールバック (`QuicConnection::custom_verify_cb`)
  には必須である。それ以外の C++ 実装のみのコールバックは、確保失敗などの
  C++ 例外を現状捕捉しておらず、`noexcept` により `std::terminate` で終了する。
  検証ヘルパーが契約上送出し得る例外についても、呼び出し元の不変条件に依存して
  到達しない前提で受容している。これらは受容している残リスクであり、捕捉の追加は
  別途の対応として扱う。戻り値が `void` のコールバックはエラー値を返せないため、
  失敗の扱いはコールバックごとに定める (乱数供給のように継続できないものは握らない)
